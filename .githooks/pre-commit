#!/bin/sh
# Block commits that reintroduce em dash (U+2014) or mailto: in shipped paths.
set -eu
ROOT="$(git rev-parse --show-toplevel)"
"$ROOT/scripts/check-no-em-dash.sh"
exec "$ROOT/scripts/check-no-mailto.sh"

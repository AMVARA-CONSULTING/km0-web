name: check-no-em-dash

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  no-em-dash:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fail on em dash (U+2014)
        run: ./scripts/check-no-em-dash.sh

# GitHub + autoagents (optional — copy to .env, never commit)

# Fine-grained PAT for Luipy56 with Issues read/write on km0-web
# GH_TOKEN=github_pat_...

AGENT_GH_REPO=AMVARA-CONSULTING/km0-web
AGENT_GIT_BRANCH=main

# Committer: set 1 to invoke 040-committer via cursor-agent
# AGENT_COMMITTER_USE_CURSOR=0

---
import type { Messages } from '../i18n/types';

interface Props {
  messages: Messages;
}

const { messages: m } = Astro.props;
---

<section id="privacy-trust" class="section-pad bg-white">
  <div class="mx-auto max-w-3xl text-center">
    <p data-reveal class="mb-4 text-sm font-medium uppercase tracking-[0.2em] text-navy/50">
      {m.privacyTrust.eyebrow}
    </p>
    <h2 data-reveal data-delay="80" class="heading-section mb-8 text-navy">
      {m.privacyTrust.heading}
    </h2>
    <p
      data-reveal
      data-delay="160"
      class="text-lg leading-relaxed text-navy/70 md:text-xl md:leading-relaxed"
    >
      {m.privacyTrust.body}
    </p>
  </div>
</section>

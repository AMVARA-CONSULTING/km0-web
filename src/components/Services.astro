---
import type { Locale } from '../i18n/types';
import type { Messages } from '../i18n/types';
import { localeHref, withHash } from '../i18n/paths';
import ServiceComingSoonModal from './ServiceComingSoonModal.astro';

interface Props {
  locale: Locale;
  messages: Messages;
}

const { locale, messages: m } = Astro.props;
const hasComingSoon = m.services.items.some((item) => item.available === false);
const cloudItem = m.services.items.find((item) => item.id === 'cloud');
const pinCta = cloudItem?.secondaryCta ?? m.hero.ctaPrimary;
const pinHref = cloudItem?.url ?? 'https://cloud.km0digital.com';
---

<section id="services" class="section-pad offer" aria-labelledby="services-heading">
  <div class="offer__layout mx-auto max-w-6xl">
    <aside class="offer__pin" data-offer-pin>
      <div class="offer__pin-surface">
        <header class="offer__header" data-reveal>
          <h2 id="services-heading" class="heading-section text-navy">
            {m.services.headingBefore}<span class="text-gradient">{m.services.headingAccent}</span>{m.services.headingAfter}
          </h2>
          <p class="offer__intro">{m.services.intro}</p>
          <p class="offer__slogan">{m.services.slogan}</p>
        </header>
        <a href={pinHref} class="offer__pin-link offer__pin-cta" data-reveal data-delay="100">
          {pinCta}
        </a>
      </div>
    </aside>

    <div class="offer__list">
      {
        m.services.items.map((item, i) => {
          const isCloud = item.id === 'cloud';
          const isEmail = item.id === 'email';
          const primaryHref = item.ctaFragment
            ? withHash(locale, item.ctaFragment)
            : item.url;

          return (
            <article
              id={isCloud ? 'km0-cloud' : isEmail ? 'km0-email' : undefined}
              data-reveal
              data-delay={String(80 + i * 80)}
              class:list={['offer-row', isCloud && 'offer-row--lead', isEmail && 'offer-row--quiet']}
            >
              <div class="offer-row__lead">
                <p class="offer-row__meta">{isCloud ? '01' : '02'}</p>
                <h3 class="offer-row__title">{item.title}</h3>
                {isCloud && item.priceFrom && (
                  <p class="offer-row__price" aria-label={item.priceAriaLabel}>
                    {item.priceFrom}
                  </p>
                )}
                {item.subtitle && <p class="offer-row__subtitle">{item.subtitle}</p>}
              </div>
              <div class="offer-row__body">
                <p class="offer-row__desc">{item.desc}</p>
                {isCloud && (
                  <a
                    href={localeHref(locale, '/tutorials/getting-started-web/')}
                    class="offer-row__text-link"
                  >
                    {m.services.cloudTutorialCta}
                  </a>
                )}
                <div class:list={['offer-row__actions', isCloud && 'offer-row__actions--paired']}>
                  {!isCloud &&
                    (item.available === false ? (
                      <button type="button" data-service-coming-soon class="btn-primary">
                        {item.cta}
                      </button>
                    ) : item.ctaFragment ? (
                      <a href={primaryHref} class="btn-primary">
                        {item.cta}
                      </a>
                    ) : (
                      <a
                        href={item.url}
                        target="_blank"
                        rel="noopener noreferrer"
                        class="btn-primary"
                      >
                        {item.cta}
                      </a>
                    ))}
                  {isCloud && (
                    <a href={localeHref(locale, '/pricing/')} class="offer-row__text-link">
                      {m.services.cloudPricingCta}
                    </a>
                  )}
                  {isCloud && item.secondaryCta && (
                    <a href={item.url} class="offer-row__text-link">
                      {item.secondaryCta}
                    </a>
                  )}
                </div>
              </div>
            </article>
          );
        })
      }
    </div>
  </div>
</section>

{hasComingSoon && <ServiceComingSoonModal messages={m} />}

<script>
  import '../scripts/service-coming-soon.ts';
</script>

<style>
  .offer__layout {
    display: grid;
    gap: 2.5rem;
  }

  @media (min-width: 1024px) {
    .offer__layout {
      grid-template-columns: minmax(0, 0.95fr) minmax(0, 1.2fr);
      gap: 3.5rem;
      align-items: start;
    }

    .offer__pin {
      position: sticky;
      top: var(--masthead-offset);
      align-self: start;
      z-index: 2;
    }

    .offer__list {
      /* Tall enough that the pin stays stuck while both rows scroll past */
      min-height: 42rem;
    }
  }

  :global(html[data-masthead='compact']) .offer__pin {
    top: var(--masthead-offset-compact);
  }

  .offer__pin-surface {
    max-width: 40rem;
    padding: 0;
    border-left: 2px solid transparent;
    transition:
      background-color var(--duration-chrome) var(--ease-out-expo),
      border-color var(--duration-chrome) var(--ease-out-expo),
      box-shadow var(--duration-chrome) var(--ease-out-expo),
      padding var(--duration-chrome) var(--ease-out-expo);
  }

  /* Stuck = Signal edge + Snow panel so the pin reads as chrome, not a CSS relic */
  @media (min-width: 1024px) {
    .offer__pin--stuck .offer__pin-surface {
      padding: 1.25rem 1.35rem 1.4rem;
      background: var(--color-snow);
      border-left-color: var(--color-signal);
      box-shadow: 0 12px 32px color-mix(in srgb, var(--color-ink) 7%, transparent);
    }
  }

  .offer__header {
    max-width: 40rem;
    margin-bottom: 0;
  }

  /* Quiet Cloud reaffirm in pin; hero + Why hold the loud Open Cloud primaries */
  .offer__pin-link {
    font-family: theme('fontFamily.sans');
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--color-ink);
    text-decoration: underline;
    text-decoration-color: color-mix(in srgb, var(--color-signal) 45%, transparent);
    text-underline-offset: 0.2em;
    transition: color 0.3s var(--ease-out-expo), text-decoration-color 0.3s var(--ease-out-expo);
  }

  .offer__pin-link:hover {
    color: var(--color-signal);
    text-decoration-color: var(--color-signal);
  }

  .offer__pin-link:focus-visible {
    outline: 2px solid var(--color-signal);
    outline-offset: 4px;
  }

  .offer__pin-cta {
    display: none;
    margin-top: 1.75rem;
  }

  @media (min-width: 1024px) {
    .offer__pin-cta {
      display: inline-flex;
    }

    .offer__pin--stuck .offer__pin-cta {
      margin-top: 1.35rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .offer__pin-surface {
      transition: none;
    }
  }

  .offer__intro {
    margin: 1rem 0 0;
    font-size: 1.125rem;
    line-height: 1.6;
    color: color-mix(in srgb, var(--color-ink) 70%, transparent);
  }

  .offer__slogan {
    margin: 0.75rem 0 0;
    font-family: theme('fontFamily.sans');
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    color: var(--color-signal);
  }

  .offer__list {
    display: grid;
    gap: 0;
    border-top: 1px solid var(--color-mist);
  }

  @media (max-width: 1023px) {
    .offer__header {
      margin-bottom: 2.5rem;
    }
  }

  .offer-row {
    display: grid;
    gap: 1.25rem;
    padding: 2rem 0;
    border-bottom: 1px solid var(--color-mist);
  }

  /* Cloud leads; Email stays quieter companion (not equal feature strips) */
  .offer-row--lead {
    padding-block: clamp(2.5rem, 4vw, 3.5rem);
  }

  .offer-row--quiet {
    opacity: 0.96;
  }

  .offer-row--quiet .offer-row__title {
    font-size: clamp(1.2rem, 2vw, 1.45rem);
    font-weight: 600;
  }

  @media (min-width: 768px) {
    .offer-row {
      grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
      gap: 2.5rem;
      align-items: start;
      padding: 2.5rem 0;
    }

    .offer-row--lead {
      padding-block: clamp(2.75rem, 4vw, 3.75rem);
    }
  }

  .offer-row__meta {
    margin: 0 0 0.5rem;
    font-family: ui-monospace, 'Cascadia Code', 'Source Code Pro', Menlo, monospace;
    font-size: 0.6875rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    color: var(--color-signal);
  }

  .offer-row__title {
    margin: 0;
    font-family: theme('fontFamily.display');
    font-size: clamp(1.5rem, 2.8vw, 2.1rem);
    font-weight: 700;
    letter-spacing: -0.025em;
    color: var(--color-ink);
  }

  .offer-row__price {
    margin: 0.5rem 0 0;
    font-family: theme('fontFamily.sans');
    font-size: 1.25rem;
    font-weight: 700;
    line-height: 1.2;
    color: var(--color-signal);
  }

  .offer-row__subtitle {
    margin: 0.5rem 0 0;
    font-family: theme('fontFamily.sans');
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--color-ink);
  }

  .offer-row__desc {
    margin: 0 0 1rem;
    line-height: 1.6;
    color: color-mix(in srgb, var(--color-ink) 70%, transparent);
  }

  .offer-row__text-link {
    font-family: theme('fontFamily.sans');
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--color-ink);
    text-decoration: underline;
    text-decoration-color: color-mix(in srgb, var(--color-signal) 40%, transparent);
    text-underline-offset: 0.18em;
  }

  .offer-row__text-link:hover {
    color: var(--color-signal);
    text-decoration-color: var(--color-signal);
  }

  .offer-row__actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.85rem 1.25rem;
    margin-top: 1rem;
  }

  .offer-row__actions--paired {
    flex-direction: row;
  }
</style>
